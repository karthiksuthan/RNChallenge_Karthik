import React from "react";
import ListingScreen from "./screens/listingScreen";

//for router or top level providers like redux provider , since not using any of those directly returning listing screen

const AppContainer = () =>{
return(
    <ListingScreen />
)
}

export default AppContainer;